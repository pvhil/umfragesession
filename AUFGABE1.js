/**
 * Aufgabe 1: „Summenberechnung eines Zahlen-Arrays“
 *
 * Beschreibung: Implementiere in JavaScript eine Funktion calculateSum(numbers),
 * die ein Array aus Ganzzahlen entgegennimmt und dessen Summe zurückgibt.
 * Kurzgesagt soll die Funktion die Summe aller Zahlen in einer Liste berechnen.
 *
 * Beispiel:
 * calculateSum([1, 2, 3, 4, 5]); // 15
 * calculateSum([10, 20, 30, 40, 50]); // 150
 * calculateSum([1, 3, 5, 7, 9]); // 25
 *
 * Führe die Aufgabe im untenstehenden "Terminal" mit dem befehl `node AUFGABE1.js` aus.
 */



// Hier implementieren
function calculateSum(numbers) {
    // Implementiere die Funktion hier, frage CoPilot nach Hilfe oder kopiere hier den ganzen Text in ChatGPT und frage nach einer Lösung

}

// Das wird ausgeführt, wenn die Funktion implementiert wurde
console.log(calculateSum([1, 2, 3, 4, 5]));
